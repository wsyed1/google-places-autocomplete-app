# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'google-places-app' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  pod 'GooglePlaces'
  pod 'GooglePlacePicker'
  pod 'GoogleMaps'
  use_frameworks!

  # Pods for google-places-app

  target 'google-places-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'google-places-appUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
